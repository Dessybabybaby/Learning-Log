# Diagrams — Week 6

Build these in [draw.io](https://app.diagrams.net/) or [Excalidraw](https://excalidraw.com/), export as `.png`, and place them here.

This week's diagram is the most important one in the whole repo — it's the first (and only) place where the tools from Weeks 1–5 are shown connected as a single system rather than reviewed in isolation.

Suggested:

- `full-stack-automation-flow.png` — trigger → automation engine → every downstream tool it touches, labeled by which week that tool was covered in
- `credential-blast-radius.png` — visualize what a compromised automation-platform credential could reach across the stack (this pairs directly with the "blast radius" risk in `security-review.md`)

Reference them in `README.md` like:

```markdown
![Full Stack Automation Flow](./diagrams/full-stack-automation-flow.png)
```
